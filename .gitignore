# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Python bytecode cache (daemon)
daemon/__pycache__/
*.pyc

# Test/QA screenshots (regenerated on demand via screenshot.sh)
out.png
splash.png
splash_*.png
usage.png
usage_*.png

# Agent context file — kept local-only, not part of the public repo
CLAUDE.md

# WIP Korean font scaffold — not applied yet (needs Pretendard-Regular.otf
# + LVGL 9 patch + fallback wiring before it actually ships)
tools/build_pretendard_fonts.sh
